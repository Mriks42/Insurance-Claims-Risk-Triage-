# Core data science
# Upper bound is deliberate. Every module selects categorical columns with
# select_dtypes(include=["object"]). pandas 3 already warns that this will stop
# matching string columns, and when it does, cat_cols becomes empty and the
# ColumnTransformer silently DROPS every categorical feature (remainder="drop")
# instead of raising — a silent accuracy collapse, not a crash. Unpin only
# alongside switching those calls to include=["object", "str"].
pandas>=2.0,<3.0
numpy>=1.24
scikit-learn>=1.3
matplotlib>=3.7
joblib>=1.3

# Gradient boosting models
xgboost>=2.0
lightgbm>=4.0
catboost>=1.2

# Imbalanced learning
imbalanced-learn>=0.11

# Hyperparameter optimisation
optuna>=3.0

# Explainability
shap>=0.44

# RAG pipeline
chromadb>=0.4
sentence-transformers>=2.2
openai>=1.0

# Environment / config
python-dotenv>=1.0

# Web application
streamlit>=1.30
plotly>=5.18

# Experiment tracking
mlflow>=2.10

# Data validation
# Upper bound: pandera 0.24 moved the DataFrameSchema/Column/Check API under
# pandera.pandas, which breaks data_validation.py's imports.
pandera>=0.18,<0.24

# Testing
pytest>=7.4
pytest-cov>=4.1

# Monitoring / drift detection
# Upper bound: evidently 0.7 removed evidently.report / evidently.metric_preset.
# Above this bound monitoring.py silently falls back to its KS-test path.
evidently>=0.4,<0.7

# Config management
pyyaml>=6.0
